# Cookie policy — draft placeholder

**Not legal advice. Requires qualified legal review and a production cookie audit.**

Status: unapproved; version and effective date not assigned.

The current platform uses secure session/security mechanisms, but counsel and
engineering must inventory every production cookie and similar technology by name,
provider, purpose, classification, duration, domain, and SameSite behavior. Define
regional consent/control requirements and third-party disclosures only after the
deployed inventory is verified.

Do not add an optional-cookie banner until preferences are actually enforced.
